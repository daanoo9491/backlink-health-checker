# Copy to .dev.vars for local Worker secrets (loaded by `npm run dev`).
# Production values are set with: npx wrangler secret put <NAME>
# Phase 1+ will add, for example:
# SESSION_SECRET=replace-with-a-long-random-string
